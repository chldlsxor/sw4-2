<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.repository.BoardDao">

	<select id="boardList" resultType="BoardDto">
		select * from (select * from board order by reg desc) where rownum between 1 and 5
	</select>
	
	<select id="addList" resultType="BoardDto" parameterType="map">
		select * from (select no, writer, content, reg, good, Row_NUMBER() OVER(order by reg desc) rnum from board) where rnum between #{start} and #{end}
	</select>
	
	<select id="getNo" resultType="int">
		 select board_seq.nextval from dual
	</select>
	
	<insert id="write" parameterType="BoardDto">
		insert into board values(#{no}, #{writer}, #{content}, sysdate, null)
	</insert>

</mapper>